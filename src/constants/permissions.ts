export enum Permissions {
    // global permissions
    GLOBAL = 'permissions:global',

    // pages permissions
    AGENCIES = 'permissions:menu-agencies',
    JOBS = 'permissions:menu-jobs',
    CANDIDATES = 'permissions:menu-candidates',

    // jobs permissions
    JOBS_ALL = 'permissions:jobs-all',
}
